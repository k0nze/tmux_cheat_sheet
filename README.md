# Tmux Cheat Sheet

## Sessions
### Create a new Session
```bash
tmux new -s [session_name]
```

### List all Sessions
```bash
tmux ls
```

### Connect to an Existing Session
```bash
tmux attach -t [session_name]
```
